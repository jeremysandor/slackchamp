// import { fromJS } from 'immutable';
// import { selectForgotPasswordSubmitDomain } from '../selectors';

describe('selectForgotPasswordSubmitDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
